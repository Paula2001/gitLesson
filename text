GIT : 

git init 
git status 
git log 
git add . OR git add <file name>
git commit -m "we should write message"
git reset --soft <hash commit before the commit we want to remove>
git restore --staged <file name>
git restore <file name>

What's a branch ? 

branch is a ref. for the HEAD or the last 

git checkout -b newbranch

git merge newbranch

git remote -vvv
